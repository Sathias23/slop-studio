[project]
name = "comfyclaude"
version = "0.1.0"
description = "MCP server for conversational image generation via ComfyUI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.1.1",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "respx>=0.22.0",
]
